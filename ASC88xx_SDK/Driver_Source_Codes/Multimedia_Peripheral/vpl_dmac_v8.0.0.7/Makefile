################################################
# Generated by Code Generator Version 4.4.0.13 #
################################################
DEBUG = n
PROFILE = n

MODULE		:= vpl_dmac.ko
HEADERFILE	:= vpl_dmac.h vpl_dmac_driver.h vpl_dmac_locals.h

# You normally should not need to change the following
ifeq ($(DEBUG),y)
	EXTRA_CFLAGS += -O -g -D_DEBUG
else
	EXTRA_CFLAGS += -O2 -DNO_DP -DNDEBUG
endif

ifeq ($(PROFILE),y)
	EXTRA_CFLAGS += -D__PROFILE__
endif

ifneq ($(KERNELRELEASE),)

	obj-m := vpl_dmac.o
	vpl_dmac-objs := vpl_dmac_driver.o vma_local/vpl_dmac_locals.o

else

	PWD := $(shell pwd)
	KERNELDIR := $(KERNELSRC)

default:
	$(MAKE) -C $(KERNELDIR) M=$(PWD) modules

clean:
	@ rm -rf   *.o *.ko .*cmd *.mod.c .tmp_versions *.order *.symvers

endif
