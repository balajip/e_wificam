#!/bin/csh
################################################
# Generated by Code Generator Version 4.4.0.14 #
################################################
DEBUG = n
PROFILE = n

MODULE		:= vma_jebe.ko
HEADERFILE	:= vma_jebe.h vma_jebe_driver.h vma_jebe_locals.h

# You normally should not need to change the following
ifeq ($(DEBUG),y)
	EXTRA_CFLAGS += -O -g -D_DEBUG
else
	EXTRA_CFLAGS += -O2 -DNO_DP -DNDEBUG
endif

ifeq ($(PROFILE),y)
	EXTRA_CFLAGS += -D__PROFILE__
endif

ifneq ($(KERNELRELEASE),)

	obj-m := vma_jebe.o
	vma_jebe-objs := vma_jebe_driver.o vma_local/vma_jebe_locals.o

else

	PWD := $(shell pwd)
	KERNELDIR := $(KERNELSRC)

default:
	$(MAKE) -C $(KERNELDIR) M=$(PWD) modules

clean:
	@ rm -rf   *.o *.ko .*cmd *.mod.c .tmp_versions *.order *.symvers

endif
