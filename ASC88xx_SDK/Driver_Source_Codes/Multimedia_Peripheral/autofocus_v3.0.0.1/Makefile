################################################
# Generated by Code Generator Version 4.2.0.6 #
################################################
DEBUG = n

MODULE		:= autofocus.ko
HEADERFILE	:= autofocus.h autofocus_driver.h autofocus_locals.h

# You normally should not need to change the following
ifeq ($(DEBUG),y)
	EXTRA_CFLAGS += -O -g -D_DEBUG
else
	EXTRA_CFLAGS += -O2 -DNO_DP -DNDEBUG
endif

ifneq ($(KERNELRELEASE),)

	obj-m := autofocus.o
	autofocus-objs := autofocus_driver.o local/autofocus_locals.o

else

	PWD := $(shell pwd)
	KERNELDIR := $(KERNELSRC)

default:
	$(MAKE) -C $(KERNELDIR) M=$(PWD) modules

clean:
	@ rm -rf *.o *.ko .*.cmd *.mod.c .tmp_versions *.order *symvers

endif
